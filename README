This is a simple example of what LaTeX can do and is a good starting point.

Compile it with:

``` bash
$ pdflatex latex_template.tex
[...]
$ bibtex latex_template
[...]
$ pdflatex latex_template.tex
[...]
$ pdflatex latex_template.tex
[...]
```

